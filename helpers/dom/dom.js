"use strict";

import * as _links from "./links";

/**
 * Export.
 * @type {{unmuteLinks?: function(*): void, muteLinks?: function(*): void}}
 */
export const links = _links;

/**
 * Aggregator
 */
export const dom = {
    links: _links,
}